<h1><%= @book.title %></h1>
<p>Author: <%= @book.author %></p>
<p>ISBN: <%= @book.isbn %></p>
<% if @book.available? %>
  <%= button_to 'Borrow Book', borrow_book_path(@book), method: :post %>
<% else %>
  <p>This book is currently unavailable.</p>
<% end %>
<%
=begin%>
 <%= link_to 'Return Book', return_book_path(@book), method: :delete, data: { confirm: 'Are you sure?' } %> 
<%
=end%>
<%= link_to 'Back to Books', books_path %>
